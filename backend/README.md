# SetFuel — backend (placeholder)

Node.js + PostgreSQL will live here in a later step.

Planned responsibilities:

- REST or tRPC API for workouts, meals, and user profile
- Google OAuth token verification and session/JWT issuance
- Migrations against PostgreSQL (e.g. Prisma or Drizzle)

Until then, the mobile app uses local placeholder state only.
